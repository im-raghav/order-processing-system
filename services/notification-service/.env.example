PORT=3005
DB_HOST=localhost
DB_PORT=3306
DB_USER=app
DB_PASSWORD=app_password
DB_NAME=notification_service
REDIS_HOST=localhost
REDIS_PORT=6379
# how often the shipped-orders scan runs, in milliseconds. Defaults to 15 minutes per the
# spec; override to something small (e.g. 5000) to demo the exactly-once behavior quickly.
NOTIFICATION_SCAN_INTERVAL_MS=900000
